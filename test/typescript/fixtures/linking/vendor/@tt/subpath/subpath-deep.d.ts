// Reached as `@tt/subpath/deep` — a specifier whose path segment after the package name
// corresponds to NO directory on disk. Resolution that concatenates the specifier onto
// the package root finds nothing here.
export declare function deepEntry(input: string): string;
export declare class DeepWorker {
  spin(n: number): number;
}
