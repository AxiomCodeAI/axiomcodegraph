// Reached as `@tt/subpath`. There is no `main` and no `types` field on this package:
// the only thing that maps the specifier to this file is the "exports" map.
export declare function rootEntry(input: string): string;
