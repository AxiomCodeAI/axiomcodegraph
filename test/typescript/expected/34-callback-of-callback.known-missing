# Accepted gaps for 34-callback-of-callback. A NEW missing edge fails the suite; a line
# here that STARTS working also fails, so the debt cannot rot.
#
# FOUR LINES, ZERO DEFECTS. The engine RESOLVES all four and names the same declaration
# at the same line; only the owner differs, and in the direction #330 deliberately chose:
#
#   engine  globals#<arrow@12>(T)        oracle  MakerConstructor#<arrow@12>(T)
#
# These are function types nested inside a CONSTRUCT SIGNATURE's parameter — the executor's
# own `resolve`/`reject`. #330 settled the anonymous-shape owner by reading the entity the
# type was written for, and for a METHOD_PARAM owner it falls through to the module, which
# is what the engine does here. The compiler side instead walks up to the enclosing
# interface. Neither is wrong; they disagree, and the disagreement costs a miss plus an
# extra per call, exactly as #322 described before #330 closed the shallower cases.
#
# Filed as the residual of that family rather than fixed here: this case exists to pin the
# RESOLUTION, and every site in it resolves. If the convention is aligned, these four lines
# start working and the suite says so.
exec#<arrow@12>(?,?) -> MakerConstructor#<arrow@12>(T)
exec#<arrow@12>(?,?) -> MakerConstructor#<arrow@12>(string)
exec#<arrow@20>(?,?) -> MakerConstructor#<arrow@12>(T)
exec#<arrow@20>(?,?) -> MakerConstructor#<arrow@12>(string)
