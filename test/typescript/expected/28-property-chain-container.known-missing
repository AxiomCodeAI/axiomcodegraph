# Accepted gaps for 28-property-chain-container — each line is an edge the TypeScript
# compiler resolves and this engine does not. A NEW missing edge fails the suite; a line
# here that STARTS working also fails, so the debt cannot rot.
#
# Not this case's subject. Inside `this.meta.active.filter((c) => c.isVirtual())` the
# callback's parameter `c` is typed by `filter`'s own signature, which is the contextual
# parameter class (#100). The same callback resolves on the two FIELD receivers in this
# case (@17, @28) and not on the getter one, so the element type reaches an arrow through
# a field and not yet through an accessor's return.
#
# What this case pins is the ACCESS itself: `this.meta.active` is typed by the getter's
# return type, so `.filter` on it names Array's declaration. Verified without the rule —
# viaGetter resolved to nothing there, while viaField, viaLocal and the annotated control
# all resolved, which is what made the accessor the cause rather than property chains in
# general.
Builder#<arrow@22>(?) -> Column#isVirtual()
