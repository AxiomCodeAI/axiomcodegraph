# A type stub with NO .py beside it: this is how PEP 561 ships types for a C
# extension. It is not importable — CPython's SOURCE_SUFFIXES is ['.py'] — so no
# function object ever exists at these names.
def fast(x: int) -> int: ...
