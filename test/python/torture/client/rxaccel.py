"""Stands in for an optional accelerator. Present here, so the `try` branch wins at
runtime and the `except` branch is the sound-set member tier 4 does not observe."""


def boost(x: int) -> int:
    return x * 4
