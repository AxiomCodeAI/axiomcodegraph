── bean_def (0) ──
── inject_point (0) ──
── di_edge (0) ──
── config_class_ref (1) ──
  properties   app.pipeline.handler               "testcases.config.PipelineStage" -> testcases.config.PipelineStage [client]
── config_key_ref (6) ──
  app.retries -> app.max-retries  (default "3")
  app.url -> app.host
  app.url -> app.port
  svc.base -> app.url
  svc.probe -> svc.base
  svc.token -> SECRET_TOKEN
── config_binding (1) ──
  app.pipeline.handler       class_binding      type     testcases.config.PipelineStage
── config_affects_method (3) ──
  app.pipeline.handler       caller           testcases.config.Runner#go()
  app.pipeline.handler       class_binding    testcases.config.PipelineStage#<init>()
  app.pipeline.handler       class_binding    testcases.config.PipelineStage#handle(String)
── config_entry_point (1) ──
  config_handler     testcases.config.PipelineStage#handle(String)
── config_unresolved  [DECLARED UNKNOWNS] (10) ──
  unbound_key                binding      app.docs
  unbound_key                binding      app.host
  unbound_key                binding      app.port
  unbound_key                binding      app.retries
  unbound_key                binding      app.url
  unbound_key                binding      svc.base
  unbound_key                binding      svc.probe
  unbound_key                binding      svc.timeout
  unbound_key                binding      svc.token
  undefined_key              placeholder  svc.token  "SECRET_TOKEN"
