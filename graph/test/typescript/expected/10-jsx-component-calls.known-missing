# Accepted gaps for 10-jsx-component-calls — each line is an edge the TypeScript compiler
# resolves and this engine does not. A NEW missing edge fails the suite;
# a line here that STARTS working also fails, so the debt cannot rot.
components#App() -> components#Badge()
components#App() -> components#Panel(Props)
