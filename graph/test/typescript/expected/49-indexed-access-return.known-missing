# EMPTY, and kept as a file on purpose: this case's debt is discharged.
#
# It used to hold `main#useGeneric() -> Cat#purr()` — `Zoo[K]` where `K extends keyof Zoo`
# — with the note that the compiler narrows K to `"cat"` from the argument at the call,
# which is generic instantiation from an argument rather than reading an index off a
# declaration, and that guessing one member would be a wrong answer rather than a missing
# one. That is still the right description of the mechanism; it is now implemented
# (generics.dl, #432) rather than declined, and the line reported NOW-FIXED, which is the
# signal this list exists to give.
