// The control: the same call through the barrel, and a named import of the merged name.
import * as core from "../core/index.js";
import { $constructor } from "../core/core.js";
export const ZodString = core.$constructor("ZodString", (inst) => { core.helper(); });
export const Direct = $constructor("Direct", () => {});
