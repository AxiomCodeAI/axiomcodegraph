# Executed edges the engine does not have, each a named gap. Format: <caller> <callee>
# (function ids as file:line:col). A new missing edge fails run.sh; a listed one that
# starts resolving fails too, so this list cannot rot.

# (PD-JS-5, `withDefault -> twice` through the `{ mapper = twice } = {}` default, was listed
# here until #673 linked the default to the binding.)

# A Proxy `get` trap manufactures the callee at runtime; nothing static names it.
lib/registry.js:10:1 lib/registry.js:9:38
lib/registry.js:10:1 lib/registry.js:9:48

# #611: a piped chain. `_transform` pushes into the next stream's `_write`, and `_flush`
# ends into its `_final`: platform-internal chaining between two piped streams, which the
# stream hook model (one trigger on one instance) does not express.
lib/streams.js:17:3 lib/streams.js:8:3
lib/streams.js:18:3 lib/streams.js:9:3

# `Function.prototype.call.bind(helper)` makes a function whose every call runs `helper`;
# the engine has no model for a bound `call`. Listed here because before #640 this direct
# call was credited through the setTimeout registration in another function and never
# appeared.
lib/direct.js:7:1 lib/direct.js:4:1
