# Executed edges the engine does not have, each a named gap. Format: <caller> <callee>
# (function ids as file:line:col). A new missing edge fails run.sh; a listed one that
# starts resolving fails too, so this list cannot rot.

# PD-JS-5: a default INSIDE a destructured parameter (`{ mapper = twice } = {}`) has no
# link to the name it initialises.
lib/functional.js:17:1 lib/functional.js:7:1

# A hand-rolled dispatcher keyed by a COMPUTED member name: `this.handlers[name]` is
# written and read with a non-literal key. dynamic_terminal by construction.
lib/bus.js:11:75 lib/bus.js:18:1
lib/registry.js:7:1 lib/registry.js:5:3

# A Proxy `get` trap manufactures the callee at runtime; nothing static names it.
lib/registry.js:10:1 lib/registry.js:9:38
lib/registry.js:10:1 lib/registry.js:9:48
