# (PD-JS-5, the defaults inside `function g({ modifier = (x) => x, second = ident } = {})`,
# was listed here until #673 linked a pattern binding's default to its references; both
# sites resolve now.)
