── framework_edge (3) ──
  task_dispatch      registered  main.enqueue_purge -> main.purge_stale   [delay]
  task_dispatch      registered  main.enqueue_rebuild -> main.rebuild_index   [apply_async]
  task_dispatch      registered  main.enqueue_report -> main.send_report   [delay]
── framework_unjoined (2) ──
     2  task_dispatch      delay
