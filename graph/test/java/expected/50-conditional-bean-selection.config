── bean_def (7) ──
  stereotype       alwaysGate             <- dep.AlwaysGate
  stereotype       fallbackGate           <- dep.FallbackGate
  stereotype       gateway                <- probe.Gateway
  stereotype       localPrimary           <- probe.LocalPrimary
  stereotype       localSecondary         <- probe.LocalSecondary
  stereotype       primaryGate            <- dep.PrimaryGate
  stereotype       profiledOnly           <- probe.ProfiledOnly
── bean_origin (7) ──
  client           gateway                <- probe.Gateway
  client           localPrimary           <- probe.LocalPrimary
  client           localSecondary         <- probe.LocalSecondary
  client           profiledOnly           <- probe.ProfiledOnly
  lib              alwaysGate             <- dep.AlwaysGate
  lib              fallbackGate           <- dep.FallbackGate
  lib              primaryGate            <- dep.PrimaryGate
── inject_point (3) ──
  field            probe.Gateway#always : dep.AlwaysGate
  field            probe.Gateway#fallback : dep.FallbackGate
  field            probe.Gateway#primary : dep.PrimaryGate
── di_edge (3) ──
  known_bean       field            probe.Gateway#always : dep.AlwaysGate <- alwaysGate (dep.AlwaysGate)
  known_bean       field            probe.Gateway#fallback : dep.FallbackGate <- fallbackGate (dep.FallbackGate)
  known_bean       field            probe.Gateway#primary : dep.PrimaryGate <- primaryGate (dep.PrimaryGate)
── config_class_ref (0) ──
── config_key_ref (0) ──
── config_binding (0) ──
── config_affects_method (0) ──
── config_entry_point (0) ──
── bean_condition (5) ──
  excluded   property  fallbackGate           app.gate.enabled = "false"
  excluded   property  localSecondary         app.local.mode = "slow"
  selected   property  localPrimary           app.local.mode = "fast"
  selected   property  primaryGate            app.gate.enabled = "true"
  undecided  profile   profiledOnly           spring.profiles.active = "batch"
── config_unresolved  [DECLARED UNKNOWNS] (3) ──
  unbound_key                binding      app.gate.enabled
  unbound_key                binding      app.local.mode
  undecided_condition        condition    probe.ProfiledOnly  "spring.profiles.active"
── remote_edge (0) ──
── remote_unserved  [SENT, NO CONSUMER HERE] (0) ──
── remote_unsent  [SERVED, NO PRODUCER HERE] (0) ──
── remote_undetermined  [DECLARED UNKNOWNS] (0) ──
