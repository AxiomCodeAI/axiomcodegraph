── bean_def (0) ──
── bean_origin (0) ──
── inject_point (0) ──
── di_edge (0) ──
── config_class_ref (0) ──
── config_key_ref (0) ──
── config_binding (0) ──
── config_affects_method (0) ──
── config_entry_point (2) ──
  grpc_service       probe.GreeterService#sayBye(String,Observer)
  grpc_service       probe.GreeterService#sayHello(String,Observer)
── bean_condition (0) ──
── config_unresolved  [DECLARED UNKNOWNS] (0) ──
── remote_edge (2) ──
  grpc    exact        probe.GreeterGrpc/sayBye           probe.GrpcProbe#async(GreeterStub) -> probe.GreeterService#sayBye(String,Observer)
  grpc    exact        probe.GreeterGrpc/sayHello         probe.GrpcProbe#blocking(GreeterBlockingStub) -> probe.GreeterService#sayHello(String,Observer)
── remote_unserved  [SENT, NO CONSUMER HERE] (0) ──
── remote_unsent  [SERVED, NO PRODUCER HERE] (0) ──
── remote_undetermined  [DECLARED UNKNOWNS] (0) ──
── persistence_query (0) ──
── persistence_entity (0) ──
── persistence_field (0) ──
── persistence_unresolved  [DECLARED UNKNOWNS] (0) ──
