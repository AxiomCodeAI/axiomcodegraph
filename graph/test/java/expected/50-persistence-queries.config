── bean_def (1) ──
  repository_proxy petTypeRepository      <- testcases.persistence.PetTypeRepository
── bean_origin (1) ──
  client           petTypeRepository      <- testcases.persistence.PetTypeRepository
── inject_point (0) ──
── di_edge (0) ──
── config_class_ref (0) ──
── config_key_ref (0) ──
── config_binding (0) ──
── config_affects_method (0) ──
── config_entry_point (0) ──
── bean_condition (0) ──
── config_unresolved  [DECLARED UNKNOWNS] (0) ──
── remote_edge (0) ──
── remote_unserved  [SENT, NO CONSUMER HERE] (0) ──
── remote_unsent  [SERVED, NO PRODUCER HERE] (0) ──
── remote_undetermined  [DECLARED UNKNOWNS] (0) ──
── persistence_query (4) ──
  inline                                  testcases.persistence.PetStore#byNickname(String)
                                           "SELECT p FROM Pet p WHERE p.nickname = :nickname"
  named        PetType.all                testcases.persistence.PetStore#all()
                                           "FROM PetType pt ORDER BY pt.name"
  named        PetType.byName             testcases.persistence.PetStore#byName(String)
                                           "SELECT pt FROM PetType pt WHERE pt.name = :name"
  spring_data                             testcases.persistence.PetTypeRepository#findAllOrdered()
                                           "SELECT pt FROM PetType pt ORDER BY pt.name"
── persistence_entity (4) ──
  testcases.persistence.Pet      testcases.persistence.PetStore#byNickname(String)
  testcases.persistence.PetType  testcases.persistence.PetStore#all()
  testcases.persistence.PetType  testcases.persistence.PetStore#byName(String)
  testcases.persistence.PetType  testcases.persistence.PetTypeRepository#findAllOrdered()
── persistence_field (9) ──
  direct     testcases.persistence.Pet#nickname     testcases.persistence.PetStore#byNickname(String)
  direct     testcases.persistence.PetType#name     testcases.persistence.PetStore#all()
  direct     testcases.persistence.PetType#name     testcases.persistence.PetStore#byName(String)
  direct     testcases.persistence.PetType#name     testcases.persistence.PetTypeRepository#findAllOrdered()
  projection testcases.persistence.Pet#id           testcases.persistence.PetStore#byNickname(String)
  projection testcases.persistence.Pet#name         testcases.persistence.PetStore#byNickname(String)
  projection testcases.persistence.PetType#id       testcases.persistence.PetStore#all()
  projection testcases.persistence.PetType#id       testcases.persistence.PetStore#byName(String)
  projection testcases.persistence.PetType#id       testcases.persistence.PetTypeRepository#findAllOrdered()
── persistence_unresolved  [DECLARED UNKNOWNS] (2) ──
  query_built_at_runtime     testcases.persistence.PetStore#ordered(String)
  undeclared_named_query     testcases.persistence.PetStore#undeclared()
