── bean_def (5) ──
  stereotype       dbStore                <- testcases.config.DbStore
  stereotype       jsonCodec              <- testcases.config.JsonCodec
  stereotype       service1               <- testcases.config.Service1
  stereotype       service2               <- testcases.config.Service2
  stereotype       xmlCodec               <- testcases.config.XmlCodec
── bean_origin (5) ──
  client           dbStore                <- testcases.config.DbStore
  client           jsonCodec              <- testcases.config.JsonCodec
  client           service1               <- testcases.config.Service1
  client           service2               <- testcases.config.Service2
  client           xmlCodec               <- testcases.config.XmlCodec
── inject_point (4) ──
  ctor_param       testcases.config.Service2#<init>(Store):store : testcases.config.Store
  field            testcases.config.Service1#codec : testcases.config.Codec
  field            testcases.config.Service1#picked : testcases.config.Codec
  field            testcases.config.Service1#store : testcases.config.Store
── di_edge (5) ──
  known_bean       ctor_param       testcases.config.Service2#<init>(Store):store : testcases.config.Store <- dbStore (testcases.config.DbStore)
  known_bean       field            testcases.config.Service1#picked : testcases.config.Codec <- jsonCodec (testcases.config.JsonCodec)
  known_bean       field            testcases.config.Service1#store : testcases.config.Store <- dbStore (testcases.config.DbStore)
  multi_bean       field            testcases.config.Service1#codec : testcases.config.Codec <- jsonCodec (testcases.config.JsonCodec)
  multi_bean       field            testcases.config.Service1#codec : testcases.config.Codec <- xmlCodec (testcases.config.XmlCodec)
── config_class_ref (0) ──
── config_key_ref (0) ──
── config_binding (0) ──
── config_affects_method (0) ──
── config_entry_point (1) ──
  bean_ctor          testcases.config.Service2#<init>(Store)
── config_unresolved  [DECLARED UNKNOWNS] (0) ──
── remote_edge (0) ──
── remote_unserved  [SENT, NO CONSUMER HERE] (0) ──
── remote_unsent  [SERVED, NO PRODUCER HERE] (0) ──
── remote_undetermined  [DECLARED UNKNOWNS] (0) ──
