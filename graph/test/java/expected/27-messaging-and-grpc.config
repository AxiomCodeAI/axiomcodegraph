── bean_def (6) ──
  stereotype       batchConsumer          <- testcases.config.BatchConsumer
  stereotype       greeterService         <- testcases.config.GreeterService
  stereotype       handler                <- testcases.config.Handler
  stereotype       mailConsumer           <- testcases.config.MailConsumer
  stereotype       orderConsumer          <- testcases.config.OrderConsumer
  stereotype       orderProducer          <- testcases.config.OrderProducer
── bean_origin (6) ──
  client           batchConsumer          <- testcases.config.BatchConsumer
  client           greeterService         <- testcases.config.GreeterService
  client           handler                <- testcases.config.Handler
  client           mailConsumer           <- testcases.config.MailConsumer
  client           orderConsumer          <- testcases.config.OrderConsumer
  client           orderProducer          <- testcases.config.OrderProducer
── inject_point (2) ──
  field            testcases.config.GreeterService#handler : testcases.config.Handler
  field            testcases.config.OrderConsumer#handler : testcases.config.Handler
── di_edge (2) ──
  known_bean       field            testcases.config.GreeterService#handler : testcases.config.Handler <- handler (testcases.config.Handler)
  known_bean       field            testcases.config.OrderConsumer#handler : testcases.config.Handler <- handler (testcases.config.Handler)
── config_class_ref (0) ──
── config_key_ref (0) ──
── config_binding (3) ──
  app.queues.mail            annotation_argument method   testcases.config.MailConsumer#onMail(String)
  app.topics.orders          annotation_argument method   testcases.config.OrderConsumer#onOrder(String)
  app.topics.orders          value_annotation   field    testcases.config.OrderProducer#topic
── config_affects_method (3) ──
  app.queues.mail            annotated_method testcases.config.MailConsumer#onMail(String)
  app.topics.orders          annotated_method testcases.config.OrderConsumer#onOrder(String)
  app.topics.orders          reads_field      testcases.config.OrderProducer#publish(String)
── config_entry_point (6) ──
  grpc_service       testcases.config.GreeterService#sayHello(String,Object)
  queue              testcases.config.BatchConsumer#onCount(Integer)
  queue              testcases.config.BatchConsumer#onText(String)
  queue              testcases.config.MailConsumer#onFallback(Object)
  queue              testcases.config.MailConsumer#onMail(String)
  queue              testcases.config.OrderConsumer#onOrder(String)
── bean_condition (0) ──
── config_unresolved  [DECLARED UNKNOWNS] (0) ──
── remote_edge (0) ──
── remote_unserved  [SENT, NO CONSUMER HERE] (0) ──
── remote_unsent  [SERVED, NO PRODUCER HERE] (2) ──
  amqp    mail-q                             testcases.config.MailConsumer#onMail(String)
  kafka   orders-v1                          testcases.config.OrderConsumer#onOrder(String)
── remote_undetermined  [DECLARED UNKNOWNS] (0) ──
