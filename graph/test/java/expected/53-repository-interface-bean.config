── bean_def (4) ──
  repository_proxy auditRepository        <- probe.AuditRepository
  repository_proxy orderRepository        <- probe.OrderRepository
  stereotype       orders                 <- probe.Orders
  stereotype       store                  <- dep.Store
── bean_origin (4) ──
  client           auditRepository        <- probe.AuditRepository
  client           orderRepository        <- probe.OrderRepository
  client           orders                 <- probe.Orders
  lib              store                  <- dep.Store
── inject_point (3) ──
  field            probe.Orders#audit : probe.AuditRepository
  field            probe.Orders#repository : probe.OrderRepository
  field            probe.Orders#store : dep.Store
── di_edge (3) ──
  known_bean       field            probe.Orders#audit : probe.AuditRepository <- auditRepository (probe.AuditRepository)
  known_bean       field            probe.Orders#repository : probe.OrderRepository <- orderRepository (probe.OrderRepository)
  known_bean       field            probe.Orders#store : dep.Store <- store (dep.Store)
── config_class_ref (0) ──
── config_key_ref (0) ──
── config_binding (0) ──
── config_affects_method (0) ──
── config_entry_point (0) ──
── bean_condition (0) ──
── config_unresolved  [DECLARED UNKNOWNS] (0) ──
── remote_edge (0) ──
── remote_unserved  [SENT, NO CONSUMER HERE] (0) ──
── remote_unsent  [SERVED, NO PRODUCER HERE] (0) ──
── remote_undetermined  [DECLARED UNKNOWNS] (0) ──
── persistence_query (0) ──
── persistence_entity (0) ──
── persistence_field (0) ──
── persistence_unresolved  [DECLARED UNKNOWNS] (0) ──
