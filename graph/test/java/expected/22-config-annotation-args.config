── bean_def (0) ──
── inject_point (0) ──
── di_edge (0) ──
── config_class_ref (4) ──
  annotation   @Codec                             "AltCodecA" -> testcases.config.AltCodecA [client]
  annotation   @Codec                             "AltCodecB" -> testcases.config.AltCodecB [client]
  annotation   @Codec                             "FooCodec" -> testcases.config.FooCodec [client]
  annotation   @Idx                               "IndexedBy" -> testcases.config.IndexedBy [client]
── config_key_ref (0) ──
── config_binding (6) ──
  svc.host                   config_properties  field    testcases.config.SvcProps#host
  svc.host                   value_annotation   field    testcases.config.Consumer#host
  svc.max-retries            config_properties  field    testcases.config.SvcProps#maxRetries
  svc.missing                value_annotation   field    testcases.config.Consumer#missing
  svc.nested.path            config_properties  field    testcases.config.Nested#path
  svc.timeout                value_annotation   field    testcases.config.Consumer#timeout
── config_affects_method (6) ──
  svc.host                   reads_field      testcases.config.Consumer#use()
  svc.host                   reads_field      testcases.config.SvcProps#host()
  svc.max-retries            reads_field      testcases.config.SvcProps#retries()
  svc.missing                reads_field      testcases.config.Consumer#use()
  svc.nested.path            reads_field      testcases.config.Nested#path()
  svc.timeout                reads_field      testcases.config.Consumer#use()
── config_entry_point (0) ──
── bean_condition (0) ──
── config_unresolved  [DECLARED UNKNOWNS] (1) ──
  undefined_key              value_annotation testcases.config.Consumer#missing  "svc.missing"
── remote_edge (0) ──
── remote_unserved  [SENT, NO CONSUMER HERE] (0) ──
── remote_unsent  [SERVED, NO PRODUCER HERE] (0) ──
── remote_undetermined  [DECLARED UNKNOWNS] (0) ──
