── bean_def (0) ──
── bean_origin (0) ──
── inject_point (0) ──
── di_edge (0) ──
── config_class_ref (0) ──
── config_key_ref (0) ──
── config_binding (0) ──
── config_affects_method (0) ──
── config_entry_point (3) ──
  service_loader     probe.Plugins.ReverseCodec#encode(String)
  service_loader     probe.Plugins.UpperCodec#<init>()
  service_loader     probe.Plugins.UpperCodec#encode(String)
── bean_condition (0) ──
── config_unresolved  [DECLARED UNKNOWNS] (0) ──
── remote_edge (0) ──
── remote_unserved  [SENT, NO CONSUMER HERE] (0) ──
── remote_unsent  [SERVED, NO PRODUCER HERE] (0) ──
── remote_undetermined  [DECLARED UNKNOWNS] (0) ──
── persistence_query (0) ──
── persistence_entity (0) ──
── persistence_field (0) ──
── persistence_unresolved  [DECLARED UNKNOWNS] (0) ──
