── bean_def (0) ──
── inject_point (0) ──
── di_edge (0) ──
── config_class_ref (1) ──
  annotation   @Tag                               "Marker" -> probe.Marker [client]
── config_key_ref (0) ──
── bean_condition (0) ──
── bean_alternative (0) ──
── config_binding (0) ──
── config_affects_method (0) ──
── config_entry_point (0) ──
── config_unresolved  [DECLARED UNKNOWNS] (0) ──
