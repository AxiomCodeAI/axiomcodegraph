── bean_def (2) ──
  stereotype       caller                 <- app.Caller
  stereotype       settings               <- app.Settings
── bean_origin (2) ──
  client           caller                 <- app.Caller
  client           settings               <- app.Settings
── inject_point (1) ──
  field            app.Caller#settings : app.Settings
── di_edge (1) ──
  known_bean       field            app.Caller#settings : app.Settings <- settings (app.Settings)
── config_class_ref (0) ──
── config_key_ref (0) ──
── config_binding (3) ──
  app.endpoint.url           value_annotation   field    app.Settings#endpointUrl
  app.hand.written           value_annotation   field    app.Settings#hand
  app.retry.enabled          value_annotation   field    app.Settings#retryEnabled
── config_affects_method (6) ──
  app.endpoint.url           caller           app.Caller#useEndpoint()
  app.endpoint.url           reads_field      generated:app.Settings#getEndpointUrl/0
  app.hand.written           caller           app.Caller#useHand()
  app.hand.written           reads_field      app.Settings#getHand()
  app.retry.enabled          caller           app.Caller#useRetry()
  app.retry.enabled          reads_field      generated:app.Settings#isRetryEnabled/0
── config_entry_point (0) ──
── bean_condition (0) ──
── config_unresolved  [DECLARED UNKNOWNS] (0) ──
── remote_edge (0) ──
── remote_unserved  [SENT, NO CONSUMER HERE] (0) ──
── remote_unsent  [SERVED, NO PRODUCER HERE] (0) ──
── remote_undetermined  [DECLARED UNKNOWNS] (0) ──
── persistence_query (0) ──
── persistence_entity (0) ──
── persistence_field (0) ──
── persistence_unresolved  [DECLARED UNKNOWNS] (0) ──
