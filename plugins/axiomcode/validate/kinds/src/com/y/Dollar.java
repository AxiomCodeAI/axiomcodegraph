package com.y;

/** A type whose NAME contains $, a common generated / internal idiom. Not Outer$Inner. */
public class Dollar$Types {
    public static String declaringClassOf(String s) { return s; }
}
