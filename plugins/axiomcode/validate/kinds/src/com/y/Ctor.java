package com.y;

/** A constructor spelled like its class: `Ctor.Ctor` is the constructor, `Ctor` is the type. */
public class Ctor {
    public Ctor(String a) { }
    public String use() { return "x"; }
}
