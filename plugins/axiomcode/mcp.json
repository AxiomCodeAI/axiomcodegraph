{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "axiomcode": {
      "type": "stdio",
      "command": "node",
      "args": ["-e", "const e=process.env;require(require('path').resolve(e.PLUGIN_ROOT||e.CLAUDE_PLUGIN_ROOT||e.CURSOR_PLUGIN_ROOT||'.','mcp','launch.js'))"]
    }
  }
}
