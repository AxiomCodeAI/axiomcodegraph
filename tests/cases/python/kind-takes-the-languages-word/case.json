{"lang": "python", "src": ".",
 "checks": [
  {"why": "--kind takes the seven words the rules are written in, and the index holds what the LANGUAGE calls it. `--kind class` on a class used to answer 'nothing of kind class named Basket in the graph' — blaming the declaration for a wrong flag value (#1107)",
   "run": ["impact", "Basket", "--kind", "class"],
   "want": ["change: class Basket", "[type]"],
   "avoid": ["nothing of kind"]},
  {"why": "the same for a constant, which the index calls `const` and the rules call a field",
   "run": ["impact", "Basket.RATE", "--kind", "const"],
   "want": ["Basket.RATE", "[field]"],
   "avoid": ["nothing of kind"]},
  {"why": "a word that is neither is refused as a bad VALUE, naming the set — not as a missing declaration, which is what `--kind wibble` used to be told word for word",
   "run": ["impact", "Basket", "--kind", "wibble"],
   "want": ["is not a kind this asks about", "method | field | type"],
   "avoid": ["nothing of kind wibble named"],
   "expect_error": true},
  {"why": "the vocabulary the rules use still works unchanged, and is what every other surface prints",
   "run": ["impact", "Basket", "--kind", "type"],
   "want": ["change: class Basket", "[type]"],
   "avoid": ["nothing of kind"]}]}
