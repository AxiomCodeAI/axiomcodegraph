{"lang": "typescript",
 "checks": [
   {"why":   "two --in roots are COMBINED, so a change spanning both is returned by one call",
    "run":   ["context", "stop formatting the amount on the subtotal", "--in", "packages/core", "--in", "packages/orders"],
    "want":  ["packages/core/src/format.ts", "packages/orders/src/invoice.ts"]},

   {"why":   "the same list written comma-separated is the same call",
    "run":   ["context", "stop formatting the amount on the subtotal", "--in", "packages/core,packages/orders"],
    "want":  ["packages/core/src/format.ts", "packages/orders/src/invoice.ts"]},

   {"why":   "ONE --in the caller supplied is knowledge and still restricts: the other root is not returned",
    "run":   ["context", "stop formatting the amount on the subtotal", "--in", "packages/orders"],
    "want":  ["packages/orders/src/invoice.ts"],
    "avoid": ["packages/core/src/format.ts"]},

   {"why":   "a misspelled root names ITSELF rather than failing the whole call behind a good one",
    "run":   ["context", "stop formatting the amount on the subtotal", "--in", "packages/core", "--in", "packages/ordrs"],
    "want":  ["no indexed file has 'packages/ordrs' in its path"],
    "expect_error": true}
 ]}
