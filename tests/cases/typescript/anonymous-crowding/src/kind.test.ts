import { TokenKind } from './kind';

declare function it(name: string, fn: () => void): void;
function use(k: TokenKind) { return k; }

  it('case 0', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 1', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 2', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 3', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 4', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 5', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 6', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 7', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 8', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 9', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 10', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 11', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 12', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 13', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 14', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 15', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 16', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 17', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 18', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 19', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 20', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 21', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 22', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 23', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 24', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 25', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 26', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 27', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 28', () => { const k: TokenKind = TokenKind.Word; use(k); });
  it('case 29', () => { const k: TokenKind = TokenKind.Word; use(k); });
